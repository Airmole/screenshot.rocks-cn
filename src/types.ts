export enum ImageFormats {
    PNG = 'png',
    JPEG = 'jpeg',
    SVG = 'svg',
}

export enum FrameType {
    Browser = 'Browser',
    Phone = 'Phone',
}

export enum CanvasBackgroundTypes {
    Image = '图片',
    Solid = '纯色',
    Gradient = '渐变',
    None = '透明',
}

export enum Browsers {
    Edge,
    Chrome,
    Firefox,
}